# Secrets — never commit
.env
.env.*
*.local.json
# routine-env-<date>.txt holds the user's full secrets — a blind `git add -A` must never stage it
skills/setup-wizard/output/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
*~

# Build artifacts
*.zip
*.plugin

# Local logs
*.log

# Python
__pycache__/
*.pyc
